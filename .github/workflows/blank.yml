name: Auto Update M3U
on:
  schedule:
    - cron: '0 3 * * *' # Her gece 03:00'te otomatik çalışır
  workflow_dispatch: # Elle tetikleme butonu açar

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' # Python sürümü 3.12'ye yükseltildi

      - name: Install yt-dlp
        run: |
          python -m pip install --upgrade pip
          pip install yt-dlp

      - name: Run Scraper Script
        run: python update.py

      - name: Commit and Push Changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-updated playlists to Master M3U8 structure"
          file_pattern: 'tonguc_egitim.m3u8 sources/*.m3u'

